import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UpdateUserScreen = ({ navigation }) => {
  return (
    <View>
      <Text>UpdateUserScreen</Text>
    </View>
  );
};

export default UpdateUserScreen;

const styles = StyleSheet.create({});
